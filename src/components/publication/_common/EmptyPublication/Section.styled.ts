import styled from '@emotion/styled'
import { Flex } from '@src/components/_core'

export const StyledIllustrationContainer = styled(Flex)(() => {
  return {
    svg: {
      height: '20rem',
    },
  }
})
